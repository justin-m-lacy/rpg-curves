import { DataTemplate } from "@/model/data-template";


export const encodeTemplates = (templates: Record<string, DataTemplate>) => {

	return templates;

}

export const encodeTemplate = (data: DataTemplate) => {

	return data;

}